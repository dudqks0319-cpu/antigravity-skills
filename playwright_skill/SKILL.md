---
name: playwright_skill
description: Playwright 웹 자동화 테스트 스킬. "테스트해줘", "자동 테스트", "웹 테스트" 요청 시 활성화.
---

# Playwright Testing 🧪

Playwright를 사용하여 웹 애플리케이션을 자동으로 테스트하는 스킬입니다.

## 테스트 워크플로우

1. **대상 확인**: 테스트할 페이지/기능 파악
2. **테스트 케이스 설계**: 검증할 항목 목록화
3. **자동화 스크립트 작성**: Playwright 코드 생성
4. **실행 및 결과 보고**: 테스트 실행 후 결과 정리

## 테스트 항목 예시

- [ ] 페이지 로딩 확인
- [ ] 버튼 클릭 동작
- [ ] 폼 입력 및 제출
- [ ] 반응형 디자인 확인

## 기본 코드 템플릿

```javascript
const { chromium } = require('playwright');

(async () => {
  const browser = await chromium.launch();
  const page = await browser.newPage();
  
  await page.goto('http://localhost:3000');
  await page.screenshot({ path: 'screenshot.png' });
  
  await browser.close();
})();
```

## 간편 대안

Playwright 설치가 어려운 경우, 브라우저 서브에이전트를 사용하여 수동 테스트 수행.
