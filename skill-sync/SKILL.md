---
name: Skill Synchronizer
description: 로컬 환경과 GitHub 저장소 간의 스킬/MCP 동기화를 똑똑하게 관리합니다.
---

# Skill Synchronizer

사용자가 여러 기기(Mac, Windows 등)에서 동일한 스킬셋을 유지할 수 있도록 돕는 스킬입니다.
단순 무식한 `git pull` 대신, 변경 사항을 분석하여 사용자에게 "질문"하고 "실행"합니다.

## 발동어
*   "스킬 동기화해줘"
*   "업데이트 확인해줘"
*   "내 깃허브랑 맞춰줘"

## 동작 로직 (Workflow)

1.  **상태 점검 (Health Check)**
    *   현재 폴더(`.gemini/antigravity`)가 git 저장소인지 확인합니다.
    *   Remote(origin)가 연결되어 있는지 확인합니다.

2.  **변경 사항 감지 (Fetch & Diff)**
    *   `git fetch origin`을 실행하여 최신 상태를 가져옵니다.
    *   `git status`와 `git diff`를 통해 로컬과 원격의 차이를 분석합니다.

3.  **인터랙티브 리포트 (Interactive Report)**
    사용자에게 다음과 같은 형식으로 보고합니다:

    > **🔄 동기화 리포트**
    >
    > **1. 🎁 GitHub 새로운 스킬 (다운로드 대기)**
    > *   `super-coder.md`: GitHub에는 있는데 여기는 없네요. **받으시겠습니까?**
    >
    > **2. 💻 로컬의 새로운 스킬 (업로드 대기)**
    > *   `my-secret-skill.md`: 이 PC에서 새로 만드셨네요. **GitHub에 올릴까요?**
    >
    > **3. ⚠️ 충돌/변경**
    > *   `prompt-architect.md`: 양쪽 내용이 다릅니다. **GitHub 버전으로 덮어쓸까요?**

4.  **실행 (Execution)**
    *   사용자의 답변(Yes/No)에 따라 `git pull`, `git add . && git commit && git push` 등을 선별적으로 수행합니다.
